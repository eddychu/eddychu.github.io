{% extends "layout-default.njk" %}

{% block content %}
<ul>
{% for post in posts %}
    <li>
     <h3>
        <a href={{"./" + post.slug + ".html"}}>
        {{ post.title }}</a>
     </h3>
    </li>
{% endfor %}
</ul>
{% endblock %} 

